package com.cloudsafe.pluginframework;

import com.cloudsafe.pluginframework.mocks.RegistryMock;
import org.junit.*;

/**
 * Created by pengl on 5/29/2016.
 */
public class PluginLoaderTest {

    /*private RegistryMock<AbstractTestClass> mock;
    private PluginLoader<AbstractTestClass> loader;

    @Before
    public void setUp() throws Exception {
        mock = new RegistryMock<>();
        loader = new PluginLoader<>(mock, AbstractTestClass.class);
    }

    @After
    public void tearDown() throws Exception {

    }

    @org.junit.Test
    public void loadPluginsFromDir() throws Exception {
        loader.loadPluginsFromDir("D:\\Work\\Programming\\Java\\out\\artifacts\\Test jars");
        Assert.assertTrue(mock.getKeys().contains("Test1") && mock.getKeys().contains("Test2") &&
                mock.getKeys().size() == 2);
    }*/

}